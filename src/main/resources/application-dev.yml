spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_cs_dev?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  jpa:
    show-sql: true

logging:
  level:
    com.ai.cs: DEBUG

app:
  storage:
    base-path: ./data-dev
